using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class tes : MonoBehaviour
{
    // Start is called before the first frame update  ok
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
